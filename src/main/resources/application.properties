spring.application.name=OsqueryMcpServer

# Server identification
spring.ai.mcp.server.name=osquery-server
spring.ai.mcp.server.version=1.0.0

# Server type (SYNC or ASYNC)
spring.ai.mcp.server.type=SYNC

# Transport configuration
spring.ai.mcp.server.stdio=true

# Logging configuration (important for STDIO mode)
spring.main.banner-mode=off
logging.console.enabled=false

# For debugging (comment out for production)
# logging.level.org.springframework.ai.mcp=DEBUG
# logging.level.com.kousenit.osquerymcpserver=DEBUG
# logging.file.name=./osquery-mcp.log