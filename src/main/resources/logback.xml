<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Suppress logback's internal status messages from polluting stdout -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <!-- STDIO mode requires clean stdout — log to file only when needed -->
    <root level="WARN"/>
</configuration>
